#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - Function
 * @s: Pointer
 * Return: Int
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	/* printf(s[2]); */
	return (i);

}
